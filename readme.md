# Url Shortner Using shortid
## Disclaimer
This is only the backend of the website, it works fine, you can fetch short url by using postman. 

### Backend 
1. Install backend dependencies:
    ```bash
    cd backend
    npm install
    ```
2. Start the backend server:
    ```bash
    npm start
    ```
